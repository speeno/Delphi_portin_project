# 포팅 완결 우선 계획 (갱신본)

_원본 ID: 672e6c6a — 2026-05-04 갱신 (라우터 권한 매핑·베이스라인 반영)_

## 목적

스모크·DB·권한 가드의 **완결성**을 먼저 올려 dfm 기반 시나리오 포팅으로 넘어갈 때 회귀를 막는다. 본 문서는 단계별 상태·산출물·미결 사항을 단일 소스로 유지한다.

## 모델 선택

| 서브태스크 | 권장 티어 | 사용자 모델 선택 메모 |
|---|---|---|
| probe 수정·슈퍼유저 ctx·DEC 한 줄 등록 | 표준 | 기본 |
| 대규모 라우터별 권한 매핑 추론·예외 분류 (완료) | 표준 | 이미 수행됨 |
| 레거시 RTF·불완전 문서 기반 해석 | 고급 권장 | 필요 시 Chat에서 고급 지정 |

**모델 선택:** 표준 행은 Cursor 기본으로 진행. 고급 권장 행만 수동으로 모델을 바꾼 뒤 해당 단계만 재실행하면 된다.

---

## 1단계: 스모크 DoD 확정 및 구현

### 1.1 완료된 작업

| 항목 | 상태 | 산출물·근거 |
|---|---|---|
| live L4 베이스라인 수집 | 완료 | `artifacts/db-smoke-baseline.json`, `artifacts/db-smoke-baseline.log` |
| 실패 그룹 목록화·런북 DoD 대조 | 완료 | L2 전 서버 OK. L4는 268건 중 100건 OK — **4대 서버 동일 패턴** → 스키마 예외가 아님 |
| 대규모 라우터별 권한 매핑 추론·예외 분류 | 완료 | `analysis/audit/db-smoke-permission-mapping.md`, `artifacts/router-auth-inventory.json`, `artifacts/permission-mapping-classification.json` |

**요약:** 실패 원인은 `Depends(get_current_user)` 직접 사용(37그룹)·`admin.user.read` 미부여(2그룹)·음성 검증의 가드 선차단(3그룹)으로 분류됨. 상세는 위 분석 메모 §2~§5.

### 1.2 정책 선택 (문서화 필수) — 권고안

| 옵션 | 내용 | 현재 권고 |
|---|---|---|
| **A** | `TestClient` 에서 `get_user_context` 외 **`get_current_user` 도 override** 하고, smoke 클레임을 **슈퍼유저**로 통일 (`permissions=['*']` 또는 `role='admin'` 등 `deps.py` 단일 규칙) | **권고** — 매트릭스 본질(SQL·스키마 회귀)과 RBAC 음성 검증을 분리하면서 변경 범위 최소 |
| **B** | L4 매트릭스를 데이터 회귀 전용으로 줄이고, 인증·권한은 별도 단위 테스트로 분리 | 대안 — DoD·DEC 문구 재정의 필요 |

**확정 (2026-05-04):** 옵션 A 채택 — DEC-062 등록, `debug/probe_backend_all_servers.py` 반영. 근거: `analysis/audit/db-smoke-permission-mapping.md` §6.

### 1.3 구현 완료·잔여

1. ~~**DEC 등록**~~ → **DEC-062** (`legacy-analysis/decisions.md`).
2. ~~**probe 구현**~~ → `_smoke_test_client`: `get_current_user` + `get_user_context`, 슈퍼유저 클레임; `probe_routes`: `headers_no_auth` 시 override 일시 제거.
3. ~~**청구 목록 매트릭스**~~ → `GET /settlement/billing` 필수 쿼리 `monthFrom`/`monthTo` 로 스모크 URL 정합 (기존 dateFrom/dateTo 는 422).
4. **음성 검증** — 현재 구현으로 `must_409`→409, `must_428`→428, `auth.expired_must_401`→401 확인됨. `variant_v4_must_422` 는 환경에서 재실행 권장.
5. **회귀 가드(선택)** — `_routes_for` vs 라우터 정적 매칭 테스트는 미도입(여전히 선택).
6. ~~**런북**~~ → `docs/db-smoke-runbook.md` DoD·DEC-062 참조 갱신.

---

## 2단계: 위치·운영 UX·계약 (별선)

- 위치 권한 수동 시나리오·재요청 UX·목적 고지는 DEC/계약에 확정 후 반영.
- 본 계획 1단계와 독립적으로 큐에 둘 수 있음.

---

## 3단계: dfm 기반 시나리오 포팅 큐

- **전제:** 1단계 스모크 DoD가 통과(또는 분리된 DoD가 문서화)된 뒤 실행.
- 규칙: `.cursor/rules/dfm-layout-input.mdc`, `migration/contracts/*.yaml` 의 `customer_variants`.

---

## 체크리스트 (상태 스냅샷)

- [x] baseline-probe-failures (live JSON·그룹화·런북 대조)
- [x] 라우터 권한 매핑·예외 분류 (분석 메모 + artifacts)
- [x] decide-smoke-dod — 옵션 A 확정 (DEC-062)
- [x] implement-smoke-auth — probe·런북·DEC 반영
- [ ] location-ops-closure
- [ ] dfm-porting-queue

---

## 참조

- `analysis/audit/db-smoke-permission-mapping.md` — 베이스라인·매트릭스·옵션 A/B 비교·후속 액션
- `docs/db-smoke-runbook.md` — 실행·DoD
- `legacy-analysis/permission-keys-catalog.md` — 권한코드 정본
- DEC-033, DEC-041 — 멀티 DB·RBAC (`legacy-analysis/decisions.md`)
